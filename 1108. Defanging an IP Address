class Solution {
public:
    string defangIPaddr(string address) {
        string c ="";
        for(int i = 0; i < address.length();i++){
            if(address[i] == '.'){
                c += "[.]";
            }else{
                c += address[i];
            }}
        return c;
    }
};
